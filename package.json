{
  "name": "datavisualization_langgraph",
  "version": "1.0.0",
  "description": "Upload SQLite or CSV files, query the data using natural language, and visualize the results. It's built with Next.js for the frontend, uses LangChain and LangGraph Cloud for text to SQL and data visualization, and includes a custom server for handling SQLite and CSV files.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
